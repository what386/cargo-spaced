fn example() {
    let first = builder
        .step_one()
        .step_two();

    consume(first);
    let second = other
        .prepare();

    let third = another
        .finish();

    consume_all(second, third);
}
